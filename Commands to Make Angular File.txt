First check all essentaial things:
1) npm install -g @angular/cli
2) ng version

Now , we create angular project:
1)ng new angularFileName
2)Adding bootstrap:-  npm install bootstrap --save

For Opening the angular file:
1) ng serve --open

To add new components:-
1) open cmd and write  ng g c nav
//which means (ng=angular  g=generate  c=component  nav=name of componenets)